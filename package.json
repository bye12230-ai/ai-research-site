{
  "name": "ai-research-knowledge-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && npx serve dist",
    "preview": "node scripts/build.mjs && npx serve dist"
  }
}
